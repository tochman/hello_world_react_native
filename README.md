## React Native with CypressIO example

This app was created ussing expo CLI

To run tests. start the app in one terminal and cypress in another tab

```
$ yarn web
```

```
$ yarn cypres
```

Execute the test in `userCanNavigate.feature.js`.